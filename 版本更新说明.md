# insigoo-memory 版本更新说明

## AI 知识管家 → SAG 知识库架构师（v2.0.0 · 开源 MIT）

> 适用对象：原「insigoo-memory · AI 知识管家」(skillhub v0.3.0) 的使用者。
> 发布日期：2026-08-31
> 技能名：`insigoo-memory`（沿用原名，但已是**完全不同方向的产品**）。

---

### 一、一句话结论

本次是**产品方向的整体替换，不是补丁升级**。

原「AI 知识管家」已停更，由全新的「SAG 知识库架构师」(v2.0.0) 完全取代。两者共用 `insigoo-memory` 这个名字，但**形态、能力、使用方式都不同**——请务必读完本文再迁移。

---

### 二、为什么替换（产品方向变了）

| | 旧版：AI 知识管家 | 新版：SAG 知识库架构师 |
|---|---|---|
| 定位 | 文件整理小工具 | 知识库建设方法论 |
| 形态 | Python CLI 命令行工具 | WorkBuddy 技能（SKILL.md + 知识包） |
| 核心 | 文件不丢、不乱、不过期 | 建得起来、查得到、管得住、说得清 |
| SAG 语义检索 | ⚠️ 仅"规划中"，未实现 | ✅ 已交付，内联 API 调用契约 |
| 授权 | 无 LICENSE | MIT 开源 |

旧版把"语义检索 + 知识诊断"当作**规划中的后端**；新版把它做成了**开箱即用的核心能力**。

---

### 三、已移除的旧功能（旧用户请留意）

以下旧能力**不再提供**，新版不接管你的文件系统：

1. **`init` / `scan` / `dashboard` / `doctor` 四个 CLI 命令** —— 新版是 WorkBuddy 技能，不是命令行工具，没有这些命令。
2. **可视化看板 `dashboard.html`** —— 新版明确"不提供文件可视化看板"，聚焦建库方法论。
3. **99 公益日等日程 / 待办提醒** —— 已移除。
4. **9 区自动分类 / 文件自动搬运** —— 新版不做文件整理，需要你按规范主动建库编译。
5. **12 议题行业知识包预置** —— 新版不绑议题，走通用方法论，更适配任意社会组织。

> ⚠️ 旧版"往里丢文件、它来分类"的心智模型在新版**不适用**。新版要求你按 `wiki_structure.md` 主动搭骨架、按 `llm_wiki_standard.md` 编译资料。

---

### 四、新增 / 强化的能力

| # | 能力 | 关键产出 |
|---|------|---------|
| 能力一 | 搭建组织 **SAG 知识库** | LLM Wiki 骨架 + 三层索引 + 维护规范 + SAG 语义检索 API 契约（`references/sag_api.md`） |
| 能力二 | 按**公益披露标准**编译历史资料 | GDT-Core 要素结构 + 15 示范任务包 + 受治理查询场景索引（六要素 + 六约束） |
| 能力三 | 基于 **SIA** 诊断组织知识 | 知识健康度卡片 + 优化建议（P0/P1/P2） |

配套自带资源（零外部依赖，装完即用）：

- `references/wiki_structure.md` — 知识库目录骨架
- `references/llm_wiki_standard.md` — LLM Wiki 五大编写原则
- `references/three_layer_index.md` — 三层索引标准（含受治理查询场景索引 v1.1）
- `references/knowledge_index_template.md` — 主索引模板
- `references/maintenance_guide.md` — 编译与维护规范（双模式检索）
- `references/gdt_design.md` — GDT-Core 治理内核设计
- `references/gdt_kb_samples/` — 15 个示范任务包
- `references/sia_core.md` — SIA 核心检查点
- `references/sag_api.md` — SAG 语义检索 API 调用契约

---

### 五、SIA 版本说明（重要）

本开源版内联的 SIA 为 **L1 逻辑自洽层**：

- 7 原则 + 三A三力四维 + 三模式（快速自检 / 完整体检 / 批量评审）+ 10 项快检 + 成熟度分级。
- **L2 指标量化层、L3 价值核算层不在本开源包范围内**，以开源技能 `insigoo-sia`(MIT) 为准。

如需 L2/L3 完整能力，另行安装 `insigoo-sia` 即可，本技能 `sia_core.md` 已与之对齐。

---

### 六、旧用户迁移指引

**1. 卸载旧版**
- 若通过 skillhub 安装：删除 `~/.workbuddy/skills/insigoo-memory/`（旧版）。
- 若曾 `pip install`：执行 `pip uninstall insigoo-memory`。

**2. 安装新版**
- 将本目录（`insigoo-memory/`）整体放入：
  - 用户级：`~/.workbuddy/skills/insigoo-memory/`
  - 或项目级：`<项目>/.workbuddy/skills/insigoo-memory/`

**3. 刷新**
- 重启 WorkBuddy 或刷新技能索引。

**4. 用新触发词调用**
- "帮我搭知识库" / "做 SAG 知识库" / "按披露标准整理资料" / "SIA 诊断组织知识"。

---

### 七、授权变更

- 旧版：**无 LICENSE**。
- 新版：**MIT 开源** —— 可自由使用、修改、分发与商用，须保留版权与许可声明（见 `LICENSE`）。

---

### 八、可选增强依赖（不装也能跑通三件事）

| 依赖 | 增强内容 | 获取方式 |
|------|---------|---------|
| `insigoo-sag` | SAG 引擎完整部署脚本 + 知识库质量巡检（Lint） | 开源 `github.com/ericyueric/insigoo-sag` |
| `insigoo-knowledge-base` | LLM Wiki 与三层索引扩展标准 | 开源 `github.com/ericyueric/insigoo-knowledge-base` |
| `insigoo-sia` | SIA 完整 L1/L2/L3 框架 | 开源 `github.com/ericyueric/insigoo-sia` |
| `GDTcreater` | GDT-DB 六件套向导 | ⚠️ 未开源（insigoo 内部技能），本技能不依赖 |


---

*insigoo 因思阁 · 让每一个社会组织都有自己的、说得清也管得住的 SAG 知识库*